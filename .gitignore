# Binaries for programs and plugins
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Compile directory from travis
build

# The binary itself
looking-glass

# JetBrains
.idea

# GoReleaser
dist