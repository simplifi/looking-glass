language: go

env:
  - GO111MODULE=on

go:
  - 1.13.x

before_script:
  - go get -u golang.org/x/lint/golint

script:
  - GOOS=linux go build -o looking-glass -i -v -ldflags="-X github.com/simplifi/looking-glass/version.Version=$(git describe --tags --dirty)" github.com/simplifi/looking-glass
  - go test -v ./...
  - golint -set_exit_status

deploy:
  provider: releases
  api_key: ${GITHUB_OAUTH_KEY}
  file:
    - looking-glass
  skip_cleanup: true
  on:
    tags: true
